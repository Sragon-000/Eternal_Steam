# 폴더 구조와 배치 기준

2026-09-14 · 종류별 상위 폴더와 기능·콘텐츠별 하위 폴더를 함께 사용한다.

```text
Assets/
├─ EternalSteam/
│  ├─ Scene/
│  │  ├─ Demo/                 # 기존 데모 3개
│  │  └─ Tests/                # FoundationSandbox, 템플릿 SampleScene
│  ├─ Code/
│  │  ├─ Building/             # 건물 정의·실행 계약·표현
│  │  ├─ Placement/            # 격자·배치·회수
│  │  ├─ Combat/               # 체력·타겟·공격
│  │  ├─ LegacyDemo/           # 기존 데모 코드와 HUD: 단계적 이전 대상
│  │  └─ Editor/               # 공통 기반 에셋 생성·검사
│  ├─ Content/
│  │  ├─ Buildings/
│  │  │  ├─ BasicTower/        # Data, Prefabs
│  │  │  ├─ DurableBuilding/   # Data
│  │  │  ├─ EmptyBuilding/     # Data
│  │  │  ├─ Common/            # 건물끼리 공유하는 Data, Prefabs
│  │  │  └─ HordeTowers/       # 기존 포탑 Art
│  │  ├─ Enemies/HordeEnemy/   # 기존 적 메시·재질
│  │  └─ Environments/HordeMaps/ # 기존 전장 재질
│  ├─ Shared/
│  │  ├─ Fonts/               # 한글 폰트와 라이선스
│  │  ├─ Materials/           # 여러 콘텐츠가 쓰는 표시·검증 재질
│  │  └─ UI/                  # Demo, Foundation, Themes
│  ├─ Settings/
│  │  ├─ Rendering/           # URP·Renderer·Volume
│  │  ├─ Input/
│  │  ├─ UI/                  # PanelSettings
│  │  └─ Catalogs/            # 콘텐츠 등록 목록
│  └─ Tests/
│     ├─ EditMode/
│     ├─ PlayMode/
│     ├─ Sandbox/             # 검증 씬 전용 진행·입력·HUD
│     └─ Templates/           # Unity 템플릿 안내 자료 보존
└─ _Recovery/                 # 사용자 로컬 복구 자료, 자동 이동·삭제 제외
Tools/                        # CLI 생성·검증 도구
Docs/                         # 기획·사용법·결과
Packages/
ProjectSettings/
```

## 신규 파일을 놓는 기준

- 관리하는 모든 `.unity` 파일은 **Assets/EternalSteam/Scene/** 아래에 둔다. 기능·콘텐츠·샘플 폴더에는 씬을 만들지 않는다. 실제 게임 씬이 생기면 `Scene/Game`을 추가한다.
- 동작을 바꾸는 코드는 `Code/<기능>`에 둔다. 공통 계약은 실제 소비 관계에 맞춰 배치하며, 아직 필요 없는 Core 폴더나 빈 기능 폴더를 미리 늘리지 않는다.
- 특정 건물·적·환경의 설정과 외형은 `Content/<종류>/<콘텐츠>`에 모은다. 하위 폴더는 필요한 `Data`, `Prefabs`, `Art`만 만든다.
- 해당 기능 안에서만 공유하면 그 기능의 Common에 둔다. 여러 영역이 사용하는 폰트·UI 스타일·재질만 Shared에 둔다.
- UI의 C#은 담당 기능 또는 검증 코드에, UXML/USS/공용 테마는 `Shared/UI`에 둔다. 같은 화면의 UXML과 USS는 함께 이동한다.
- 검증 전용 코드는 `Tests/Sandbox`에 둔다. Editor 전용 도구와 자동 테스트는 별도 어셈블리로 관리한다.
- 기존 데모는 동작 변경 없이 `Code/LegacyDemo`에 모았다. 이 폴더 분리는 전투 코드의 기능별 리팩터링 완료를 의미하지 않는다.
- 외부 플러그인은 Packages 또는 제공자가 요구하는 경로를 사용한다. 프로젝트 소유 코드와 섞지 않는다.

## 어셈블리 의존성

- `EternalSteam.Runtime`: Code의 Building·Placement·Combat. LegacyDemo와 Sandbox를 참조하지 않는다.
- `EternalSteam.LegacyDemo`: 기존 데모만 포함한다. GUID와 namespace를 유지한다.
- `EternalSteam.Sandbox`: 검증 진행·입력·HUD. Runtime을 참조한다.
- `EternalSteam.Editor`: Runtime과 Sandbox를 참조하는 Editor 전용 도구.
- EditModeTests와 PlayModeTests: Runtime과 Sandbox를 참조한다.

폴더는 파일 소유권을 보여 주고, 어셈블리는 실제 코드 의존 경계를 제한한다. 폴더 이동만으로 게임 규칙이나 데이터 구조를 바꾸지 않는다.

## 이동 시 지킬 사항

Unity AssetDatabase 이동으로 파일 GUID를 유지한다. 씬 빌드 경로, 문자열 기반 AssetDatabase 경로, UXML/USS의 경로, 생성·검증 도구, 현재 실행 문서를 함께 갱신한다. 기존 씬 이름과 빌드 순서, PlayerPrefs 저장 키는 유지한다.

외부 작업 폴더의 중복 Docs와 기획 원본, 사용자 `_Recovery`는 이번 정리 대상이 아니다. 과거 검증 보고서는 당시 경로·검증 기록으로 보존하고, 현재 실행 위치는 이 문서와 실행 안내를 따른다.

## 선택 모듈 확장 위치

- `Code/Combat`: 공격 실행 방식과 적중 효과, 상태 지속시간.
- `Code/Progression`: 넥서스 등록·파괴와 강화 제한.
- `Code/Economy`: 자원 잔량·용량, 저장·생산·변환.
- `Code/Movement`: 선택형 이동 방벽 등록.
- `Content/Buildings/ModuleSamples/Data`: 확장 검증용 조합 에셋.
- `Settings/Catalogs/ModuleCatalog.asset`: 확장 카탈로그.
- `Scene/Tests/ModuleSandbox.unity`: 확장 검증 씬.

개발 의존성은 [개발 모듈 경계](개발_모듈_경계.md)를 따른다. 공통 계약은 `Code/Contracts`에 두며 Combat·Progression·Economy·Movement·Presentation을 각각 별도 어셈블리로 관리한다.

## 레거시 호환 계층 내부 분리

`Code/LegacyDemo/Definitions`는 기존 맵·포탑 정의, `Deployment`는 장치 독립 배치 상태, `Presentation`은 입력 변환·카메라·미리보기를 담당한다. 각각 별도 어셈블리이며 LegacyDemo가 연결한다. 레거시 배치 테스트는 `Tests/Legacy/EditMode`에서 관리한다.

`Code/LegacyDemo/Buildings`는 기존 데모와 공통 BuildingWorld를 연결하는 어댑터·포탑 팩터리다. `EternalSteam.LegacyBuildings` 어셈블리는 HordeSimulation을 참조하지 않는다. 연결 테스트는 `Tests/Legacy/Buildings`에서 관리한다.

`Code/LegacyDemo/Combat`은 적 상태·공간 인덱스·피해 판정을 소유하는 `EternalSteam.LegacyCombat` 어셈블리다. 적 인스턴싱은 `Presentation/HordeEnemyRenderer`, 포탑 공격 연결과 연출은 `Buildings/HordeTowerCombat`·`HordeTowerEffects`에 둔다. 순수 전투 검증은 `Tests/Legacy/Combat`, 통합 도구는 `Tools`에서 관리한다. [전투 분리](레거시_전투_분리.md) 참고.

Terrain 기반 테스트 공간은 `Tests/OpenWorld` 코드, `Shared/UI/OpenWorld` UI, `Content/Environments/TestGround` 지형 에셋, `Scene/Tests/OpenWorldSandbox.unity` 씬으로 나눈다. [사용 안내](오픈월드_테스트_공간.md) 참고.
