# 폴더 재구성 검증

2026-09-14 · Unity 6000.3.14f1 / macOS arm64.

- Unity AssetDatabase로 71개 에셋 이동. 이동 전후 파일 GUID 71개 모두 일치.
- 관리 씬 5개를 `Assets/EternalSteam/Scene` 아래로 이동. 모든 씬의 누락 스크립트 0개.
- 기존 빌드 씬 순서·씬 이름과 PlayerPrefs 키 유지.
- 공통 Runtime, LegacyDemo, Sandbox 및 Editor·테스트 어셈블리 분리.
- EditMode 14/14 통과: [결과](folder-editmode.json).
- PlayMode 3/3 통과: [결과](folder-playmode.json).
- VerifyFoundationRegression: 세 맵의 로비·출격·시작·클리어·보상 중복 방지·저장·해금·패배·재도전·설치 한도 저장 통과.
- CreateHordeDemo, CreateHordeMaps: 새 경로 기준 dry-run 컴파일 통과.
- 현재 코드·스타일·도구에서 이전 에셋 경로 정리. 메타 파일 누락 없음.

사용자가 열어 둔 원본 Editor를 종료하지 않고 임시 복사본에서 검증한 뒤 결과를 원본에 반영했다. 새 에셋 트리와 검증 복사본의 파일 일치를 확인했다. `_Recovery`와 외부 문서·기존 미추적 연구 문서는 수정하지 않았다.

이번 변경은 폴더 소유권과 어셈블리 경계 정리다. 기존 데모 내부 로직의 기능별 분해나 콘텐츠 밸런스 변경은 포함하지 않는다.
